import {StyledFooter, StyledButton} from "./style"

function MainFooter() {

  return (
    <div><StyledFooter>
      footer
    </StyledFooter><StyledButton>
        button
      </StyledButton></div>
  );
}

export default MainFooter;


